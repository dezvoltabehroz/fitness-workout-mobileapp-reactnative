import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        flex: 1
    },
    grayText: {
        fontSize: 12,
        color: 'gray',
        marginTop: 5
    }
})